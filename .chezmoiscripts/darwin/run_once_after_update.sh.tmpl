#!/bin/zsh

if ! command -v brew &> /dev/null; then
  echo -e "\n * * Install Homebrew requirements on macOS * * \n"
  xcode-select --install

  echo -e "\n * * Wait until the Xcode Command Line Tools are installed * * \n"
  until xcode-select -p &> /dev/null; do
    sleep 5
  done

  echo -e "\n * * Install Homebrew * * \n"
  /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
  eval "$(/opt/homebrew/bin/brew shellenv)"
fi

# shellcheck disable=SC2016 source=/dev/null
if ! grep -Fxq 'eval "$(/opt/homebrew/bin/brew shellenv)"' ~/.zprofile; then
  echo -e "\n * * Add Homebrew to shell configuration * * \n"
  echo >> ~/.zprofile
  echo 'eval "$(/opt/homebrew/bin/brew shellenv)"' >> ~/.zprofile
  source ~/.zprofile
fi

if ! command -v mise &> /dev/null; then
  echo -e "\n * * Install mise * * \n"
  brew install mise
fi

# shellcheck disable=SC2016 source=/dev/null
if ! grep -Fxq 'eval "$(mise activate zsh)"' ~/.zshrc; then
  echo -e "\n * * Add mise to shell configuration * * \n"
  echo >> ~/.zshrc
  echo 'eval "$(mise activate zsh)"' >> ~/.zshrc
  source ~/.zshrc
fi

mise install --cd "$HOME"

if ! command -v git &> /dev/null; then
  echo -e "\n * * Install Git * * \n"
  brew install git
fi

if ! command -v git-flow &> /dev/null; then
  echo -e "\n * * Install Git Flow Next * * \n"
  brew install gittower/tap/git-flow-next
fi

if ! command -v micro &> /dev/null; then
  echo -e "\n * * Install micro * * \n"
  brew install micro
fi

if ! command -v code &> /dev/null; then
  echo -e "\n * * Install Visual Studio Code * * \n"
  brew install --cask visual-studio-code
fi

if [ ! -d "/Applications/Bitwarden.app" ]; then
  echo -e "\n * * Install Bitwarden Desktop * * \n"
  brew install --cask bitwarden
fi

# shellcheck disable=SC2016 source=/dev/null
if ! grep -q "SSH_AUTH_SOCK" ~/.zshrc; then
  echo -e "\n * * Configure SSH agent for Bitwarden * * \n"
  echo >> ~/.zshrc
  echo 'export SSH_AUTH_SOCK=$HOME/.bitwarden-ssh-agent.sock' >> ~/.zshrc
  source ~/.zshrc
fi

if [ ! -e ~/.ssh ]; then
  echo -e "\n * * Create SSH folder * * \n"
  mkdir ~/.ssh
  chmod 700 ~/.ssh
fi

known_hosts_updated=false

if [ ! -e ~/.ssh/known_hosts ] || ! grep -q "^bitbucket.org ssh-ed25519 " ~/.ssh/known_hosts; then
  echo -e "\n * * Add Bitbucket to SSH known hosts * * \n"
  ssh-keyscan -t ed25519 bitbucket.org 2>/dev/null >> ~/.ssh/known_hosts
  known_hosts_updated=true
fi

if [ ! -e ~/.ssh/known_hosts ] || ! grep -q "^github.com ssh-ed25519 " ~/.ssh/known_hosts; then
  echo -e "\n * * Add GitHub to SSH known hosts * * \n"
  ssh-keyscan -t ed25519 github.com 2>/dev/null >> ~/.ssh/known_hosts
  known_hosts_updated=true
fi

if [ ! -e ~/.ssh/known_hosts ] || ! grep -q "^gist.github.com ssh-ed25519 " ~/.ssh/known_hosts; then
  echo -e "\n * * Add GitHub Gist to SSH known hosts * * \n"
  ssh-keyscan -t ed25519 gist.github.com 2>/dev/null >> ~/.ssh/known_hosts
  known_hosts_updated=true
fi

if [ ! -e ~/.ssh/known_hosts ] || ! grep -q "^gitlab.com ssh-ed25519 " ~/.ssh/known_hosts; then
  echo -e "\n * * Add GitLab to SSH known hosts * * \n"
  ssh-keyscan -t ed25519 gitlab.com 2>/dev/null >> ~/.ssh/known_hosts
  known_hosts_updated=true
fi

if $known_hosts_updated; then
  echo >> ~/.ssh/known_hosts
fi

if ! command -v gh &> /dev/null; then
  echo -e "\n * * Install GitHub CLI * * \n"
  brew install gh
fi

if ! gh auth status -h github.com &> /dev/null; then
  echo -e "\n * * Authenticate with GitHub * * \n"
  gh auth login -h github.com -p ssh --scopes "admin:public_key admin:ssh_signing_key gist read:org repo" --skip-ssh-key
fi

echo -e "\n * * Update allowed signers from GitHub * * \n"
GH_USER=$(gh api user --jq '.login')
gh api "users/$GH_USER/ssh_signing_keys" --paginate --jq '.[].key' > '{{ .ssh.allowedSigners }}'

if [ ! -e ~/Library/Fonts/CaskaydiaMonoNerdFontMono-Regular.ttf ]; then
  echo -e "\n * * Download Nerd Fonts CaskaydiaMono * * \n"
  curl -fsSLO https://github.com/ryanoasis/nerd-fonts/releases/latest/download/CascadiaMono.tar.xz

  echo -e "\n * * Extract Nerd Fonts CaskaydiaMono * * \n"
  mkdir -p ~/Library/Fonts
  tar -xf CascadiaMono.tar.xz -C ~/Library/Fonts --exclude='CaskaydiaMonoNerdFont-*.ttf' --exclude='CaskaydiaMonoNerdFontPropo-*.ttf' --exclude='LICENSE' --exclude='README.md'

  echo -e "\n * * Remove Nerd Fonts CaskaydiaMono downloaded file * * \n"
  rm -f CascadiaMono.tar.xz
fi

# shellcheck disable=SC2016 source=/dev/null
if ! grep -Fxq 'eval "$(zoxide init zsh --cmd cd)"' ~/.zshrc; then
  echo -e "\n * * Add zoxide to shell configuration * * \n"
  echo >> ~/.zshrc
  echo 'eval "$(zoxide init zsh --cmd cd)"' >> ~/.zshrc
  source ~/.zshrc
fi

# shellcheck disable=SC2016 source=/dev/null
if ! grep -Fxq 'eval "$(atuin init zsh --disable-up-arrow)"' ~/.zshrc; then
  echo -e "\n * * Add atuin to shell configuration * * \n"
  echo >> ~/.zshrc
  echo 'eval "$(atuin init zsh --disable-up-arrow)"' >> ~/.zshrc
  source ~/.zshrc
fi

if [ ! -e ~/source ]; then
  echo -e "\n * * Create folder for source code subfolders * * \n"
  mkdir -p ~/source
fi

if [ ! -e ~/source/repos ]; then
  echo -e "\n * * Create subfolder for source code repositories * * \n"
  mkdir -p ~/source/repos
fi

if ! command -v node &> /dev/null; then
  echo -e "\n * * Install Node.js * * \n"
  brew install node@22
fi

if [[ "$(npm config get ignore-scripts --global)" != "true" ]]; then
  echo -e "\n * * Configure NPM to Ignore Scripts * * \n"
  sudo npm config set ignore-scripts true --global
fi

exit 0
